# --- local cluster config (NEVER commit; copy from config.example.yaml) ---
# config.yaml holds account paths; config.<PROJECT>.yaml are per-screen copies.
config.yaml
config.*.yaml
!config.example.yaml

# --- runs, caches, outputs ---
runs/
out/
logs/
seq_cache/
msa_cache/
*.permodel.csv

# --- large prediction outputs / structures ---
*.npz
*.cif
*.pdb
*.bin.zst
!tests/data/*.pdb     # keep tiny test fixtures
!tests/data/*.cif     # keep tiny test fixtures

# --- python ---
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
*.egg
build/
dist/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- virtualenvs ---
# NB env/ is a TRACKED source directory (env/af3lis.yml is the conda spec).
# Only the bootstrap-generated bits are ignored.
.venv/
venv/
env/.conda/
env/activate.sh

# --- editors / OS ---
.DS_Store
.idea/
.vscode/
*.swp
*~

# --- rendered SLURM artifacts (per-run; live under outdir but guard root too) ---
align.sbatch
infer.sbatch
SUBMIT_CMDS.sh
af3_json_list.txt
RUNBOOK.md
